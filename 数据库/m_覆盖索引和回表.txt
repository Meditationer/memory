mysql建表时的pk主键，NN 非空，UQ 唯一。

回表查询：
	InnoDB存储引擎的两种索引实现：有且只有一个聚集索引(通常是pk)，没定义第一个NN UQ.
	聚集索引存储行记录，普通索引存储主键。回表就是先定位主键值，再通过主键值定位行

索引覆盖：
	mysql官网没有这个概念。
	实现：将被查询的字段，建立到联合索引里去。第一种要查询的字段建立普通索引，联合主键一起查询。没试过
		将单列索引升级为联合索引